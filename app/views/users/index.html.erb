<h1>All users</h1>

<ul data-role="listview" data-theme="g">
  <% @users.each do |user| %>
    <li>
	<a href="<%= user_path(user) %>">
		<%= gravatar_for user, :size => 80 %>
		<h3><%= user.name %></h3>
   	</a>
   	</li>
  <% end %>
</ul>