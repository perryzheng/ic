<% if signed_in? %>
	<%= render 'shared/user_info' %>
	<a href="<%= chapters_path %>" data-icon='grid' data-role='button' >Check out the Disciples Class</a>
	<h1>What's up?</h1>
	<%= render 'shared/micropost_form' %>
	<%= render 'shared/feed' %>
<% else %>
	<% render 'static-home' %>
<% end %>